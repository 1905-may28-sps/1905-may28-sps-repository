package com.revature.introduction;

public class UnderstandingExceptions {
	/*
	 * Exceptions in Java objects that are thrown
	 * up the stack at runtime halting the execution of
	 * your application when something goes wrong
	 * they are typically 
	 * 
	 * An exception can be "handled" by either catching it
	 * at the moment it's thrown or by having the method that 
	 * it is thrown in further throw it up
	 * the stack or "propagate" it. (also known as ducking)
	 * 
	 */

}
